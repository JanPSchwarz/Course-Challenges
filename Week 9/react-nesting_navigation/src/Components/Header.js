// export default function Header({ children }) {
//   return <header className="header">{children}</header>;
// }

export default function Header({ children }) {
  return <header className="header">{children}</header>;
}
